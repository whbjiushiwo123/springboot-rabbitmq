<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:p="http://www.springframework.org/schema/p"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns:rabbit="http://www.springframework.org/schema/rabbit"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        https://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        https://www.springframework.org/schema/context/spring-context.xsd
        http://www.springframework.org/schema/aop
        http://www.springframework.org/schema/aop/spring-aop.xsd
        http://www.springframework.org/schema/rabbit
        http://www.springframework.org/schema/rabbit/spring-rabbit-2.0.xsd http://code.alibabatech.com/schema/dubbo http://code.alibabatech.com/schema/dubbo/dubbo.xsd">


    <aop:aspectj-autoproxy />
    <context:component-scan base-package="com.whb" />
    <context:property-placeholder location="classpath:rabbitmq.properties" file-encoding="UTF-8" ignore-unresolvable="true"/>
    <!-- rabbitmq配置 -->
    <bean id="rabbitConnectionFactory" class="org.springframework.amqp.rabbit.connection.CachingConnectionFactory">
        <constructor-arg value="${spring.rabbitmq.host}" />
        <property name="username" value="${spring.rabbitmq.username}" />
        <property name="password" value="${spring.rabbitmq.password}" />
        <property name="channelCacheSize" value="8" />
        <property name="port" value="${spring.rabbitmq.port}"/>
        <property name="publisherConfirmType" value="${spring.rabbitmq.publisherConfirmType}" />
        <property name="virtualHost" value="${spring.rabbitmq.virtualHost}" />
    </bean>
    <!-- spring 的 rabbit-admin -->
    <rabbit:admin connection-factory="rabbitConnectionFactory" />
    <rabbit:queue id="reduce_account"  name="reduce_account" durable="true" />
    <!-- 将队列和交换器由键绑定 -->
    <rabbit:direct-exchange name="reduce-account-exchange"
            xmlns="http://www.springframework.org/schema/rabbit"
                            durable="true" >
        <bindings>
            <binding queue="reduce_account" key="depot.account"></binding>
        </bindings>
    </rabbit:direct-exchange>

    <bean id="rabbitTemplate" class="org.springframework.amqp.rabbit.core.RabbitTemplate">
        <constructor-arg ref="rabbitConnectionFactory" />
        <property name="confirmCallback" ref="confirmCallBack" />
        <property name="returnCallback" ref="returnCallback" />
    </bean>

    <dubbo:reference id="testService" interface="com.whb.service.TestService" />
    <dubbo:application name="consumer" />
    <dubbo:registry protocol="zookeeper" address="zookeeper://39.101.143.248:2181" />
</beans>